export const AUTH_ENDPOINTS = {
    LOGIN: '/auth/login',
  };
  
  // A medida que implementes cada dominio, sumás su grupo acá.
  // Ejemplo de cómo se vería PRODUCTO_ENDPOINTS cuando llegues a esa parte
  // (path variables como funciones, igual que BY_ID):
  //
  // export const PRODUCTO_ENDPOINTS = {
  //   BASE: '/productos',
  //   BY_ID: (id: number) => `/productos/${id}`,
  // };